{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "node-terminal",
          "name": "Start - Dev",
          "request": "launch",
          "command": "pnpm run start:dev",
          "cwd": "${workspaceFolder}"
      }
  ]
}
